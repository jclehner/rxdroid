To build, first run ./scripts/download-thirdparty-sources to download
the NumberPicker library. You must then create an Eclipse Android project
from existing source using ./thirdparty/NumberPicker as base directory.
